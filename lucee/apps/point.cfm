<cfscript>

    function getUserData(userId) {

        var userData;
        return userData;
    }

    userId = 1;
    userData = getUserData(userId);

    writeOutput("User Name: " & userData.name);
</cfscript>
